# Fast-API---Uppercase
📌 What is an API? API stands for: Application Programming Interface It allows two systems to communicate with each other. Eg: Mobile app → sends request → server Server → sends response → app 📌 What is FastAPI? FastAPI is a modern Python FW used to build APIs quickly and effici. It is designed for: High perf., fast development, and auto. docum.
